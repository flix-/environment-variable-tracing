extern char *getenv(const char *);

int
main()
{
    int a,b,c,d,e;

    if (a) {
        do {
            if ((getenv("gude") && a && b && c) || e) {
                if (a) {
                    do {
                        int a = 0;
                    } while (0);
                } else {
                    int b = 1;
                }
            } else if (a || b || c) {
                int d = a;
            }
            int tainted = 1;
        } while (getenv("gude"));
        int end = 1;
    }

    int ut = 0;

    return 0;
}

